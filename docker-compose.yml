services:
  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      - PORT=3001
      - CORS_ORIGIN=${APP_URL:-http://localhost}
    volumes:
      - backend_config:/app/config

  frontend:
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - backend

volumes:
  backend_config:
